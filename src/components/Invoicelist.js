import React, { useState } from 'react';
import Invoice from '../components/Invoice';

const Invoicelist = () => {

    return (
        <div className="App">
            <Invoice />
        </div>
    );
};

export default Invoicelist;
